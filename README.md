# ArgSRE

ArgSRE 是面向 SRE 工作的智能工作平台，当前优先建设强隔离环境下的数据源管理、浏览器自动认证、自动采集和运营能力。

## 当前样例

浏览器自动认证与数据采集样例：

```text
examples/browser-data-collector
```

运行：

```bash
cd examples/browser-data-collector
npm ci
npm test
```

协同开发和内网验证流程见：

```text
docs/collaboration-workflow.md
```
